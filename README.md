# Jarvis's Daily Work Log

📅 简洁、美观的工作日志系统，基于 React + Vite + TypeScript 开发，部署在 Cloudflare Pages。

## 📁 项目结构

```
Jarvis-daily-work-log/
├── data/                    # 工作日志数据文件
│   ├── manifest.json        # 自动生成，包含所有日志信息
│   └── 2026-02-01.md      # 每天的工作日志（按日期命名）
├── scripts/                 # 构建脚本
│   └── build.cjs           # 生成 manifest.json
├── src/                     # React 源代码
│   ├── components/          # React 组件
│   │   ├── Header.tsx      # 顶部组件
│   │   └── DayCard.tsx     # 日志卡片组件
│   ├── utils/               # 工具函数
│   │   └── markdown.ts     # Markdown 解析
│   ├── App.tsx             # 主应用
│   └── main.tsx            # 入口文件
├── index.html              # 主页面
├── package.json            # 依赖配置
├── vite.config.ts          # Vite 配置
└── README.md               # 说明文档
```

## 🚀 如何添加新的工作日志

### 1. 创建新的md文件

在 `data/` 目录下创建新的md文件，文件名格式为 `YYYY-MM-DD.md`，例如 `2026-02-02.md`。

### 2. 文件格式

```markdown
---
date: 2026-02-02
---

## 工作情况

| 任务 | 状态 | 备注 |
|------|------|------|
| 任务名称 | ✅ 已完成 | 备注 |

## 详细记录

- 09:00 - 完成某项工作
- 10:00 - 完成另一项工作

## 备注

当日备注信息
```

### 3. 重新构建manifest

```bash
cd /Users/linzhipeng/Jarvis-workspace/Jarvis-daily-work-log
node scripts/build.cjs
```

### 4. 提交到GitHub

```bash
git add data/
git commit -m "feat: Add work log for 2026-02-02"
git push origin main
```

Cloudflare Pages 会自动部署更新。

## 🛠️ 技术栈

- **前端框架**: React 19 + TypeScript
- **构建工具**: Vite 6
- **样式**: Tailwind CSS 3.4
- **部署**: Cloudflare Pages
- **Markdown解析**: 自定义解析器

## ✨ 特性

- ✅ 响应式设计，支持移动端
- ✅ 玻璃态UI设计
- ✅ 卡片可折叠展开
- ✅ 自动展开最新日志
- ✅ Markdown 内容实时解析
- ✅ 表格和列表支持
- ✅ 静态部署，无需后端

## 📦 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 🌐 部署

项目通过 Cloudflare Pages 自动部署。

1. 推送到 GitHub
2. Cloudflare Pages 自动构建
3. 访问 https://ce9e891a.jarvis-daily-work-log.pages.dev

## 📝 维护

每天添加新的工作日志时：
1. 创建新的md文件（格式参考上方）
2. 运行构建脚本生成manifest.json
3. 提交到GitHub

## 🔧 故障排查

### 页面显示为空

检查以下几点：

1. **manifest.json 是否存在**
   ```bash
   ls -la data/manifest.json
   ```

2. **React 控制台是否有错误**
   - 打开浏览器开发者工具
   - 查看 Console 选项卡
   - 检查是否有 404 错误或解析错误

3. **GitHub 仓库文件是否正确推送**
   ```bash
   git status
   git log --oneline -3
   ```

4. **Cloudflare Pages 构建状态**
   - 访问 Cloudflare Dashboard
   - 查看 Pages 项目构建日志
   - 确认构建成功

---

由 OpenClaw + Jarvis 自动维护
